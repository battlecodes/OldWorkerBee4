<Window x:Class="WorkerBee.Views.MainWindowView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WorkerBee.Views"
        xmlns:viewmodels="clr-namespace:WorkerBee.ViewModels"
        xmlns:components="clr-namespace:WorkerBee.Components"
        mc:Ignorable="d"
        Title="MainWindowView"
        Height="550"
        Width="900"
        WindowStartupLocation="CenterScreen">
    
    <Grid>

        <Grid.Background>
            <ImageBrush
                ImageSource="\rsc\img\bgd\white-gray-hexagon-pattern-background\background.jpg"
                Opacity="1"/>
        </Grid.Background>
        
        
        <!-- Main Content -->
        <ContentControl
            Content="{Binding CurrentMainViewModel}"
            Margin="15">
            <ContentControl.Resources>
                <DataTemplate DataType="{x:Type viewmodels:LayoutViewModel}">
                    <components:Layout/>
                </DataTemplate>
            </ContentControl.Resources>
        </ContentControl>
        
    </Grid>
    
</Window>
